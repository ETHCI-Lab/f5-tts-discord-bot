import { Message } from "discord.js";
import { CommandRouter } from "./interface/commandRouter.js";


export const commandRouter: CommandRouter = {
    dispatch: async (message: Message) => {

    }
}